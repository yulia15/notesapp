<h2>Добавление заметки</h2>
<%= form_for :note, url: notes_path do |f| %>
	<p>
		Название<br>
		<%= f.text_field :title %>
	</p>
	<p>
		Текст заметки<br>
		<%= f.text_area :body %>
	</p>
	<p>
		<%= f.submit %>
	</p>
	<% end %>